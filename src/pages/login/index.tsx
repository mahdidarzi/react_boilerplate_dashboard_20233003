import React from 'react';

interface loginProps {
  label?: string;
 
}

export const Login: React.FC<loginProps> = ({ label }) => {
 
  return (
    <div>
      <>Login</>
    </div>
  );
};
